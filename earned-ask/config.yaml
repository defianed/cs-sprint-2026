# Who you are
account_name: Acme Corp
csm_name: Sarah Johnson
slack_channel: "#cs-alerts"

# What tools you use (provider=manual means use sample_data/ files)
data_source: demo_stack
crm_provider: demo_stack
transcript_provider: demo_stack
support_provider: demo_stack

# Workflow-specific settings
min_nps_to_ask: 8           # only trigger earned ask if NPS is at or above this threshold
